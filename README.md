### Provides a set of tools for building schema files using xml that can be converted to sql files suitable for installing in a real database
* Supports PostgreSQL, MySQL, Microsoft SQL Server, H2, HSQL
